# 竞品差距执行记录（2026-03-04）

## 对标结论到执行动作

1. 竞品普遍支持多种 CI 消费格式（SARIF、平台原生报告）
2. 竞品普遍把“接入成本”压到最低（开箱即用工作流）
3. 竞品普遍具备持续分发入口（文档站/落地页自动部署）

## 本轮已落地升级

1. 新增 `junit` 输出格式，支持测试看板消费
2. 新增 `scan:junit` 脚本，直接产出 `reports/scan.junit.xml`
3. 新增 `GitHub Pages` 自动部署工作流，持续发布 `site/` 宣传页

## 价值

1. 企业 CI 平台集成门槛下降（JUnit 与既有测试面板兼容）
2. 开源项目可持续对外展示（Pages 自动化，不依赖手工部署）
3. 与头部项目的“分发与接入能力”差距进一步缩小

## 下一轮建议

1. 增加 `helpUri/tags` 到 SARIF 规则元数据
2. 增加 `--format junit --fail-on p1` 的 Action 输出工件
3. 设计规则包机制（security/release 两个官方包）

