# Release Notes: v0.3.0

Date: 2026-03-04

## Highlights

1. Execution observability upgrade for scan reports:
   - stage timing (`analysis.timing`)
   - slow file hotspots (`analysis.hotspots.slowFiles`)
   - throughput summary (`analysis.summary`)
2. Fleet execution summary grouping for JSON output:
   - `executionSummary.timing`
   - `executionSummary.hotspots`
   - `executionSummary.stability`
3. Bilingual marketing site:
   - Chinese pages under `site/`
   - English pages under `site/en/`
4. Reporting schema migration guide:
   - `docs/guides/report-schema-migration.md`

## Compatibility

1. CLI commands and flags remain unchanged.
2. Existing report fields remain available; grouped fields are added for forward-compatible integrations.

## Validation

1. `npm run lint` passed
2. `npm test` passed
3. `npm run build` passed
