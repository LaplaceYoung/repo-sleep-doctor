name: Deploy Site

on:
  push:
    branches:
      - main
    paths:
      - "site/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure Pages is enabled
        id: ensure_pages
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            try {
              await github.rest.repos.getPages({ owner, repo });
              core.info("GitHub Pages is already enabled.");
            } catch (error) {
              if (error.status === 404) {
                core.info("Pages not found; creating site with workflow build type.");
                await github.rest.repos.createPagesSite({
                  owner,
                  repo,
                  build_type: "workflow"
                });
                core.info("GitHub Pages has been enabled.");
              } else {
                throw error;
              }
            }

      - name: Setup Pages
        id: setup_pages
        continue-on-error: true
        uses: actions/configure-pages@v5

      - name: Upload artifact
        if: steps.setup_pages.outcome == 'success'
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        if: steps.setup_pages.outcome == 'success'
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Pages deployment note
        if: steps.setup_pages.outcome != 'success'
        run: echo "::warning::GitHub Pages is not fully configured yet. Enable Pages source to GitHub Actions in repository settings."
